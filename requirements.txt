# pigment-gwas Python dependencies
# Install with: pip install -r requirements.txt

# Upper bounds cap the next major so a future release can't silently break the pull.
# Tested on pandas 3.0.x, requests 2.34.x, Python 3.12 (see .python-version).
pandas>=2.0,<4
requests>=2.31,<3     # live GWAS Catalog pull (scripts/gwas_catalog.py)
jupyter>=1.0,<2       # Quarto executes the .qmd workbooks via the Jupyter kernel
plotly>=5.0,<7        # interactive end-of-workbook visualizations (Manhattan, effect sizes)
