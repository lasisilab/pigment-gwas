# pigment-gwas Python dependencies
# Install with: pip install -r requirements.txt

pandas>=2.0
requests>=2.31     # live GWAS Catalog pull (scripts/gwas_catalog.py)
jupyter>=1.0       # Quarto executes the .qmd workbooks via the Jupyter kernel
