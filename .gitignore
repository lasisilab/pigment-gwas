# Quarto build artifacts
/_site/
/.quarto/
**/*.quarto_ipynb
# The rendered site is built & deployed by GitHub Actions (.github/workflows/publish.yml),
# not committed. Render locally with `quarto render` to preview it in docs/.
/docs/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# macOS
.DS_Store

# NOTE: data/ and output/ are deliberately COMMITTED — they are the atlas itself
# (the pulled catalog data, its version history, and the harmonized compendium).
